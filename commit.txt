feat(pair-detail): add live candle updates via kline WebSocket

- Add useBinanceKline hook subscribing to kline_{interval} stream
- Use useRef for ISeriesApi to access series across useEffects
- Update last candle in real-time via series.update()
- Recreate WebSocket on interval change via dependencies
- Apply same reconnect pattern (exponential backoff, MAX_ATTEMPTS)
- Handle UTCTimestamp branded type with as UTCTimestamp assertion
- Math.floor for time consistency between REST and WebSocket